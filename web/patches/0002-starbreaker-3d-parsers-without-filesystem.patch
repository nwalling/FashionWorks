diff --git a/crates/starbreaker-3d/Cargo.toml b/crates/starbreaker-3d/Cargo.toml
index 360ef2b..363f2c5 100644
--- a/crates/starbreaker-3d/Cargo.toml
+++ b/crates/starbreaker-3d/Cargo.toml
@@ -10,7 +10,7 @@ starbreaker-datacore = { path = "../starbreaker-datacore" }
 starbreaker-p4k = { path = "../starbreaker-p4k" }
 starbreaker-cryxml = { path = "../starbreaker-cryxml" }
 starbreaker-dds = { path = "../starbreaker-dds" }
-starbreaker-blend = { path = "../starbreaker-blend" }
+starbreaker-blend = { path = "../starbreaker-blend", optional = true }
 gltf-json = { version = "1", features = ["names", "extras", "KHR_lights_punctual", "KHR_materials_transmission", "KHR_materials_ior", "KHR_materials_emissive_strength", "KHR_materials_volume", "KHR_texture_transform"] }
 image = { version = "0.25", default-features = false, features = ["png"] }
 exr = "1.74"
@@ -27,3 +27,9 @@ quick-xml = "0.37"
 
 [dev-dependencies]
 env_logger = "0.11"
+
+[features]
+default = ["blend", "pipeline"]
+blend = ["dep:starbreaker-blend", "pipeline"]
+# The mesh/skeleton parsers without the filesystem-bound export pipeline.
+pipeline = []
diff --git a/crates/starbreaker-3d/src/animation/mod.rs b/crates/starbreaker-3d/src/animation/mod.rs
index 69fa2fd..1663340 100644
--- a/crates/starbreaker-3d/src/animation/mod.rs
+++ b/crates/starbreaker-3d/src/animation/mod.rs
@@ -32,16 +32,20 @@ pub use pose::{
 pub use caf::parse_caf;
 pub use dba::parse_dba;
 pub mod serialise;
+#[cfg(feature = "pipeline")]
 pub mod mannequin;
+#[cfg(feature = "pipeline")]
 pub mod matching;
 pub use serialise::{
     annotate_animations_json_with_blend_modes, classify_bone_blend_modes,
     clip_to_json, database_to_animations_json, dump_database_to_json,
     sanitize_clip_filename, split_clip_for_sidecar, BoneBlendMode,
 };
+#[cfg(feature = "pipeline")]
 pub use mannequin::{
     annotate_animation_fragments_json, dump_mannequin_adb_to_json,
 };
+#[cfg(feature = "pipeline")]
 pub use matching::{
     caf_anchored_remap_decisions, extract_animations_for_skeleton_json,
     ClipMatchDecision,
diff --git a/crates/starbreaker-3d/src/lib.rs b/crates/starbreaker-3d/src/lib.rs
index 2dbdfbe..7478d71 100644
--- a/crates/starbreaker-3d/src/lib.rs
+++ b/crates/starbreaker-3d/src/lib.rs
@@ -1,21 +1,34 @@
+// Parsers first: these read bytes and nothing else, so they work anywhere.
+pub mod animation;
+pub mod chrparams;
 pub mod dequant;
-pub(crate) mod decomposed;
 pub mod error;
-pub(crate) mod gltf;
-pub(crate) mod included_objects;
 pub mod ivo;
 pub mod mtl;
 pub mod nmc;
-pub(crate) mod pipeline;
 pub mod skeleton;
-pub(crate) mod socpak;
 pub mod types;
-pub mod animation;
-pub mod chrparams;
+
+// The rest reads a memory-mapped P4K or writes export files, neither of which
+// a browser has. Gating it keeps the parsers usable on wasm32, where
+// `MappedP4k` does not exist and `zstd` has no target.
+#[cfg(feature = "pipeline")]
+pub(crate) mod decomposed;
+#[cfg(feature = "pipeline")]
+pub(crate) mod gltf;
+#[cfg(feature = "pipeline")]
+pub(crate) mod included_objects;
+#[cfg(feature = "pipeline")]
+pub(crate) mod pipeline;
+#[cfg(feature = "pipeline")]
+pub(crate) mod socpak;
+#[cfg(feature = "pipeline")]
 pub mod validation;
 
 pub use error::Error;
+#[cfg(feature = "pipeline")]
 pub use included_objects::{IncludedObject, IncludedObjects};
+#[cfg(feature = "pipeline")]
 pub use pipeline::{
     DecomposedExport, ExportFormat, ExportKind, ExportOptions, ExportResult, ExportedFile,
     ExportedFileKind, MaterialMode,
@@ -24,6 +37,7 @@ pub use pipeline::{
     resolve_loadout_meshes, socpaks_to_glb,
 };
 pub use types::Mesh;
+#[cfg(feature = "pipeline")]
 pub use validation::{ValidationReport, validate_decomposed_export};
 
 use starbreaker_chunks::ChunkFile;
@@ -68,6 +82,7 @@ pub(crate) fn parse_skin_with_options(data: &[u8], use_model_bbox: bool) -> Resu
 /// Parse a `.skin`/`.cgf` IVO file and convert to GLB in one step.
 /// If `metadata` is provided (the primary `.cgf`/`.skin` bytes), NMC transforms
 /// are parsed from it and applied to the scene graph.
+#[cfg(feature = "pipeline")]
 pub fn skin_to_glb(data: &[u8], metadata: Option<&[u8]>) -> Result<Vec<u8>, Error> {
     let mesh = parse_skin(data)?;
     let root_nmc = metadata
diff --git a/crates/starbreaker-3d/src/pipeline/mod.rs b/crates/starbreaker-3d/src/pipeline/mod.rs
index 4000aa4..738190e 100644
--- a/crates/starbreaker-3d/src/pipeline/mod.rs
+++ b/crates/starbreaker-3d/src/pipeline/mod.rs
@@ -41,7 +41,11 @@ pub use self::vehicle::*;
 mod glb_assembly;
 pub use self::glb_assembly::{assemble_glb_with_loadout, assemble_glb_with_loadout_with_progress};
 pub(crate) use self::glb_assembly::path_is_shield_related;
+// The Blender writer pulls in `zstd`, a C library that has no wasm32 target.
+// Nothing in a browser writes .blend files, so it is optional.
+#[cfg(feature = "blend")]
 mod blend_assembly;
+#[cfg(feature = "blend")]
 pub use self::blend_assembly::write_decomposed_export_blend;
 
 
